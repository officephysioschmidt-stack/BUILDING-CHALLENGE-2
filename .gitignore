# Secrets — API-Keys NIEMALS committen
.env
*.key
credentials*.json

# Abhängigkeiten / Umgebungen
node_modules/
.venv/
venv/
__pycache__/

# Generierte / gescrapte Daten (werden vom Scraper frisch erzeugt)
data/

# Generierte Dashboard (wird vom Build-Script frisch erzeugt)
dashboard/index.html

# System
.DS_Store
*.log
